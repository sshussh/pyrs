[package]
name="codegen"
version = "0.21.0"
edition="2024"
links = "codegen_shim"

[dependencies]
ir = { path = "../ir" }

[dev-dependencies]
parser = { path = "../parser" }
semantic = { path = "../semantic" }

[build-dependencies]
cmake = "0.1.57"
