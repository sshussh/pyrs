[package]
name="codegen"
version = "0.138.2"
edition="2024"
links = "codegen_shim"

[dependencies]
ir = { path = "../ir" }

[dev-dependencies]
parser = { path = "../parser" }
semantic = { path = "../semantic" }

[build-dependencies]
cmake = "0.1.57"
